[package]
name = "cuda-fpga-toolkit"
version = "0.2.0"
edition = "2021"
description = "TLMM encoding and Hilbert mapping — uses cuda-equipment"

[dependencies]
# cuda-equipment = { path = "../cuda-equipment" }
